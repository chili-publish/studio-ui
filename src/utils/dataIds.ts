import { getDataId, getDataTestId } from '@chili-publish/grafx-shared-components';

export const getDataIdForSUI = (name: string) => getDataId(name, 'sui');
export const getDataTestIdForSUI = (name: string) => getDataTestId(name, 'sui');
